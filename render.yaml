services:
  - type: web
    name: garage-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: "cd .. && npm install"
    startCommand: "node index.js"
    healthCheckPath: /api/health
